import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://watanabe3tipapa.github.io',
  base: '/standard-libraries-now',
  outDir: '../dist',
  publicDir: './public',
})
